#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int razaaq;

	char ade;


	for (razaaq = 48; razaaq < 58; razaaq++)

		putchar(razaaq);

	for (ade = 'a'; ade <= 'f'; ade++)

		putchar(ade);

	putchar('\n');

	return (0);
}
