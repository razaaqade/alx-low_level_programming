#include <stdio.h>

/**
 * main - Entry point
 *
 * Return:Always 0 (success)
 */
int main(void)
{
	char razaaq;

	for (razaaq = 'z'; razaaq >= 'a'; razaaq--)

		putchar(razaaq);

	putchar('\n');

	return (0);
}
