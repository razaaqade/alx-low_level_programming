#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int razaaq;

	for (razaaq = 0; razaaq < 10; razaaq++)

	{
		printf("%d", razaaq);
	}

	printf("\n");

	return (0);
}
