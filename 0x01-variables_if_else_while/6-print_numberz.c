#include <stdio.h>

/**
 * main - entry point
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int razaaq;

	for (razaaq = 48; razaaq < 58; razaaq++)

		putchar(razaaq);

	putchar('\n');

	return (0);
}
