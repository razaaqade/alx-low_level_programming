#include <stdio.h>

/**
 * main - Entry point
 * Author: Razaaqade
 * Date: 09/09/2022
 * Return: Always 0 (Success)
 */
int main(void)
{
	char sam;

	for (sam = 'a'; sam <= 'z'; sam++)
	{
		putchar(sam);
	}
	putchar('\n');

	return (0);
}
